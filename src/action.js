// Action
export  const increaseAction = 'increase';
export  const sub = 'sub';
export const  IncreaseAction = (increaseAction) =>({
    type:increaseAction
});

export const Sub = (sub)=>({
    type:sub
})

